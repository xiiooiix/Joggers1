<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/default_background"
    tools:context="kkt.com.joggers.fragment.BoardFragment">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rcView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="30dp" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/floating_action"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="40dp"
        android:layout_marginEnd="40dp"
        android:foreground="@drawable/ic_menu"
        android:scaleX="1.3"
        android:scaleY="1.3"
        app:elevation="8dp"
        app:fabSize="mini" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/write_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/floating_action"
        android:layout_alignStart="@+id/floating_action"
        android:foreground="@drawable/ic_pencil"
        android:scaleX="0"
        android:scaleY="0"
        app:elevation="0dp"
        app:fabSize="mini" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/my_board_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/floating_action"
        android:layout_alignStart="@+id/floating_action"
        android:foreground="@drawable/myboard"
        android:scaleX="0"
        android:scaleY="0"
        app:elevation="0dp"
        app:fabSize="mini" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/my_heart_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/floating_action"
        android:layout_alignStart="@+id/floating_action"
        android:foreground="@drawable/heart"
        android:scaleX="0"
        android:scaleY="0"
        app:elevation="0dp"
        app:fabSize="mini" />

</RelativeLayout>